import 'package:flutter/material.dart';

class DicoveryTab extends StatelessWidget {
  const DicoveryTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Discovery Tab'),
      ),
    );
  }
}
